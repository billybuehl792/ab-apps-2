export enum EQueryType {
  List = "list",
  Count = "count",
  Detail = "detail",
}

export enum EObjectChangeType {
  Create = "create",
  Update = "update",
  Delete = "delete",
}

export enum EOrderingDirection {
  ASC = "asc",
  DESC = "desc",
}
