export enum ClientListOrdering {
  Email = "email",
  FirstName = "first_name",
  LastName = "last_name",
}

export enum ClientListFilters {
  Email = "email",
  FirstName = "first_name",
  LastName = "last_name",
}
